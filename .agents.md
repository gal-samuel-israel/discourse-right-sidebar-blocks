# Agent Notes

- Source the component version from `package.json`.
- Keep the version tail in `yymmdd` format.
- When making repo updates, if the version tail date is not the current `yymmdd`, update it.
- Example: `1.0.260217` has the `yymmdd` tail `260217`.
